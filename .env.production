# Production template.
#
# All secrets and the tunnel URL are expected from the real environment
# (Kubernetes secret, AWS Secrets Manager, Doppler, GitHub Actions, ...).
# If any required secret is missing, `Settings._require_secrets_in_production`
# aborts startup with an explicit list — better than a 500 on the first call.
#
# Only non-secret defaults live in this file.

# --- App ---
ENV=production
LOG_LEVEL=${LOG_LEVEL:-INFO}
PUBLIC_BASE_URL=${PUBLIC_BASE_URL:-}
MAX_CONCURRENT_CALLS=${MAX_CONCURRENT_CALLS:-50}

# --- Anthropic ---
ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
ANTHROPIC_MODEL=${ANTHROPIC_MODEL:-claude-opus-4-7}
ANTHROPIC_MAX_TOKENS=${ANTHROPIC_MAX_TOKENS:-1024}
ANTHROPIC_TIMEOUT_S=${ANTHROPIC_TIMEOUT_S:-30}
ANTHROPIC_MAX_RETRIES=${ANTHROPIC_MAX_RETRIES:-3}

# --- Deepgram (STT) ---
DEEPGRAM_API_KEY=${DEEPGRAM_API_KEY:-}
DEEPGRAM_MODEL=${DEEPGRAM_MODEL:-nova-3}
DEEPGRAM_LANGUAGE=${DEEPGRAM_LANGUAGE:-multi}
DEEPGRAM_ENDPOINTING_MS=${DEEPGRAM_ENDPOINTING_MS:-300}

# --- ElevenLabs (TTS) ---
ELEVENLABS_API_KEY=${ELEVENLABS_API_KEY:-}
ELEVENLABS_VOICE_ID=${ELEVENLABS_VOICE_ID:-21m00Tcm4TlvDq8ikWAM}
ELEVENLABS_MODEL=${ELEVENLABS_MODEL:-eleven_turbo_v2_5}
ELEVENLABS_TIMEOUT_S=${ELEVENLABS_TIMEOUT_S:-30}

# --- Twilio ---
TWILIO_ACCOUNT_SID=${TWILIO_ACCOUNT_SID:-}
TWILIO_AUTH_TOKEN=${TWILIO_AUTH_TOKEN:-}
TWILIO_FROM_NUMBER=${TWILIO_FROM_NUMBER:-}
# Signature validation is always on in production (non-opt-in).
TWILIO_VALIDATE_SIGNATURE=true

# --- Storage ---
DATABASE_URL=${DATABASE_URL:-}
REDIS_URL=${REDIS_URL:-}
