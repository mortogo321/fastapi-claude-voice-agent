[project]
name = "fastapi-claude-voice-agent"
version = "0.1.0"
description = "Realtime voice AI agent: FastAPI + Claude Opus 5 + Twilio Media Streams + Deepgram + ElevenLabs"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Somchai Juntapracon" }]
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.3",
    "anthropic>=0.122.0",
    "httpx[http2]>=0.28.1",
    "websockets>=17.0.1",
    "twilio>=9.11.0",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.15.0",
    "sqlalchemy[asyncio]>=2.0.52",
    "asyncpg>=0.31.0",
    "alembic>=1.19.1",
    "redis>=8.1.0",
    "structlog>=26.1.0",
    "python-multipart>=0.0.32",
    "audioop-lts>=0.2.2; python_version >= '3.13'",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=7.1.0",
    "ruff>=0.16.3",
    "mypy>=2.3.0",
    "httpx>=0.28.1",
]

[build-system]
requires = ["hatchling>=1.32.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = [
    "E", "W",     # pycodestyle
    "F",          # pyflakes
    "I",          # isort
    "B",          # bugbear
    "UP",         # pyupgrade
    "ASYNC",      # async-await correctness
    "S",          # bandit
    "PTH",        # pathlib over os.path
    "RUF",        # ruff-specific
]
ignore = [
    "S101",       # assert in tests
    "B008",       # FastAPI Depends in defaults
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S", "ASYNC"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-ra --strict-markers --strict-config"

[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["pydantic.mypy"]
